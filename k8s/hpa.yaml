apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: legal-term-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: legal-term-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
